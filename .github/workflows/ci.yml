name: ci

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install wheel tooling
        run: python -m pip install --upgrade build
      - name: Unit and incident tests
        run: python -m unittest discover -s tests -v
        env:
          PYTHONPATH: src
      - name: Incident tests (explicit package discovery)
        run: python -m unittest discover -s tests/incidents -t tests -v
        env:
          PYTHONPATH: src
      - name: Unit and incident tests (simulated Win32 provider)
        if: runner.os == 'Linux'
        run: |
          python -m unittest discover -s tests -v
          python -m unittest discover -s tests/incidents -t tests -v
        env:
          PYTHONPATH: src
          ORCHESTRATE_BOOTSTRAP_PROVIDER: simulated-win32
      - name: Compile
        run: python -m compileall -q src tests
      - name: Build and isolated install smoke
        shell: bash
        run: |
          python -m build
          python -m venv smoke-env
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            smoke-env/Scripts/python -m pip install dist/*.whl
            smoke-env/Scripts/orchestrate --help
            smoke-env/Scripts/orchestrate doctor --help
            smoke-env/Scripts/orchestrate-wsl --help
          else
            smoke-env/bin/python -m pip install dist/*.whl
            smoke-env/bin/orchestrate --help
            smoke-env/bin/orchestrate doctor --help
            smoke-env/bin/orchestrate-wsl --help
          fi
